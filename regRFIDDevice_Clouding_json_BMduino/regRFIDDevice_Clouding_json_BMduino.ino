/*****************************************************************
 * 程式名稱: regRFIDDevice_Clouding_json_BMduino.ino
 * 功能說明: RFID 門禁裝置註冊程式 (JSON 格式版本) - 將裝置資訊以 JSON 格式上傳至雲端後台
 * 
 * 【程式概述】
 *   本程式為 RFID 門禁系統的裝置註冊程式，採用 JSON 資料格式，
 *   主要目的為：
 *   1. 初始化 BMC81M001 WiFi 模組並連接到無線熱點
 *   2. 取得裝置的 MAC 位址作為唯一識別碼
 *   3. 將裝置資訊以 JSON 格式上傳至雲端後台
 *   4. 完成門禁裝置的雲端註冊流程
 * 
 * 【JSON 格式說明】
 *   JSON (JavaScript Object Notation) 是一種輕量級的資料交換格式，
 *   具有結構清晰、易於閱讀和解析的優點，廣泛用於 Web API 資料傳輸。
 *   
 *   JSON 資料格式範例：
 *   {
 *     "device": {
 *       "mac": "AA:BB:CC:DD:EE:FF",
 *       "ssid": "MyHomeWiFi",
 *       "ip": "192.168.1.100",
 *       "type": "rfid_gateway",
 *       "version": "1.0.0",
 *       "timestamp": "2024-01-15 10:30:00"
 *     }
 *   }
 * 
 * 【為什麼使用 JSON 格式？】
 *   ┌─────────────────────────────────────────────────────────┐
 *   │ 優點                    │ 說明                          │
 *   ├─────────────────────────┼───────────────────────────────┤
 *   │ 結構化資料              │ 可表示複雜的巢狀結構          │
 *   │ 語言中立                │ 各種程式語言皆可解析          │
 *   │ 易於閱讀                │ 人類可讀性高，便於除錯        │
 *   │ 擴充性強                │ 可輕鬆新增欄位而不影響舊版    │
 *   │ 標準化                  │ 已成為 Web API 的事實標準     │
 *   │ 資料型別支援            │ 支援字串、數字、布林、陣列等  │
 *   └─────────────────────────────────────────────────────────┘
 * 
 * 【RFID 門禁系統架構說明】
 *   本程式是 RFID 門禁系統的裝置端註冊程式，整體系統架構如下：
 *   
 *   ┌─────────────────────────────────────────────────────────────┐
 *   │                    雲端伺服器 (Cloud Server)                │
 *   │  ┌─────────────────────────────────────────────────────┐   │
 *   │  │  接收 JSON 格式註冊資料：                           │   │
 *   │  │  - MAC 位址（裝置唯一識別碼）                       │   │
 *   │  │  - SSID（連接的 WiFi 熱點）                         │   │
 *   │  │  - IP 位址（網路定位）                              │   │
 *   │  │  - 裝置類型（rfid_gateway）                         │   │
 *   │  │  - 韌體版本                                         │   │
 *   │  │  - 註冊時間戳                                       │   │
 *   │  │  儲存至資料庫，回傳註冊結果                         │   │
 *   │  └─────────────────────────────────────────────────────┘   │
 *   └─────────────────────────────────────────────────────────────┘
 *                                    ▲
 *                                    │ HTTP POST (JSON)
 *                                    │ Content-Type: application/json
 *                                    │
 *   ┌─────────────────────────────────────────────────────────────┐
 *   │                    BMduino + BMC81M001                      │
 *   │  ┌─────────────────────────────────────────────────────┐   │
 *   │  │  1. 連接 WiFi 熱點                                  │   │
 *   │  │  2. 取得 MAC 位址（裝置唯一識別碼）                 │   │
 *   │  │  3. 取得 SSID、IP 等網路資訊                        │   │
 *   │  │  4. 組裝 JSON 格式資料                              │   │
 *   │  │  5. 透過 HTTP POST 上傳 JSON 註冊資訊               │   │
 *   │  └─────────────────────────────────────────────────────┘   │
 *   └─────────────────────────────────────────────────────────────┘
 *                                    ▲
 *                                    │ 連接 RFID 讀取器
 *                                    │
 *   ┌─────────────────────────────────────────────────────────────┐
 *   │                    RFID 讀取模組                           │
 *   │  (後續可讀取卡片進行門禁驗證)                               │
 *   └─────────────────────────────────────────────────────────────┘
 * 
 * 【MAC 位址在門禁系統中的應用】
 *   MAC 位址作為門禁裝置的唯一識別碼，具有以下特性：
 *   
 *   ┌─────────────────────────────────────────────────────────┐
 *   │ 特性                    │ 說明                          │
 *   ├─────────────────────────┼───────────────────────────────┤
 *   │ 唯一性                  │ 全球唯一，不會重複            │
 *   │ 不可變性                │ 硬體燒錄，無法偽造            │
 *   │ 識別依據                │ 雲端後台辨識裝置身分          │
 *   │ 權限關聯                │ 與門禁權限設定綁定            │
 *   │ 資料庫主鍵              │ 儲存裝置設定和紀錄的主鍵      │
 *   └─────────────────────────────────────────────────────────┘
 * 
 * 【JSON 版本與傳統版本差異】
 *   ┌─────────────────────────────────────────────────────────┐
 *   │ 特性          │ JSON 版本              │ 傳統 GET 版本  │
 *   ├───────────────┼───────────────────────┼────────────────┤
 *   │ 請求方式      │ HTTP POST             │ HTTP GET       │
 *   │ 資料格式      │ JSON 結構化資料       │ URL 查詢字串   │
 *   │ 可讀性        │ 高，結構清晰          │ 中             │
 *   │ 擴充性        │ 強，可自由新增欄位    │ 需修改 URL     │
 *   │ 資料量限制    │ 較大                   │ 較小 (URL 限制)│
 *   │ 安全性        │ 較高 (POST)           │ 較低 (GET)     │
 *   │ 複雜資料      │ 支援巢狀結構          │ 不支援         │
 *   └─────────────────────────────────────────────────────────┘
 * 
 * 【註冊流程】
 *   1. 裝置開機 → 2. 連接 WiFi → 3. 取得 MAC 位址 → 
 *   4. 組裝 JSON 註冊資料 → 5. HTTP POST 發送 → 6. 雲端解析 JSON 並回傳結果
 * 
 * 【應用場景】
 *   1. 門禁裝置初始化：新安裝的門禁讀卡機首次上線註冊
 *   2. 裝置更換：更換故障裝置時，重新註冊新的 MAC 位址
 *   3. 批量部署：多個門禁點同時安裝，自動完成註冊
 *   4. 遠端管理：雲端後台可遠端查看所有註冊裝置狀態
 *   5. 權限同步：註冊後可下載該門禁點的權限設定
 *   6. 裝置資訊更新：定期更新裝置狀態（IP 變化、韌體版本等）
 * 
 * 【硬體架構】
 *   - 控制板: BMduino (Arduino 相容開發板)
 *   - WiFi 模組: BMC81M001 (ESP8266 核心)
 *   - RFID 模組: 可選配，用於讀取卡片 (本版本僅做裝置註冊)
 *   - 通訊介面: 硬體序列埠 Serial1 (速率 115200 bps)
 *   - 監控介面: USB 序列埠 Serial (速率 9600 bps)
 * 
 * 【通訊架構示意圖】
 *   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐
 *   │   BMduino   │◄───────►│  BMC81M001  │◄───────►│   WiFi AP   │
 *   │  (主控板)   │ Serial1 │  (WiFi模組) │ 無線網路 │  (無線熱點) │
 *   └─────────────┘         └─────────────┘         └─────────────┘
 *         ▲                        │                        │
 *         │ Serial (USB)           │ HTTP POST (JSON)      │
 *         ▼                        ▼                        ▼
 *   ┌─────────────┐         ┌─────────────┐         ┌─────────────┐
 *   │    電腦     │         │   MAC 位址  │         │   雲端      │
 *   │ (序列監控)  │         │   JSON 資料 │         │   伺服器    │
 *   └─────────────┘         └─────────────┘         └─────────────┘
 * 
 * 【執行流程】
 *   1. 開機後熄滅 LED 作為狀態指示
 *   2. 初始化 USB 序列埠 (9600 bps) 用於輸出監控資訊
 *   3. 呼叫 initWiFi() 執行 WiFi 模組初始化與連線
 *   4. 顯示連接的 SSID 和取得的 IP 位址
 *   5. 取得裝置的 MAC 位址（裝置唯一識別碼）
 *   6. 檢查 WiFi 連線狀態，成功則執行雲端註冊
 *   7. 註冊完成後進入空迴圈（單次註冊模式）
 * 
 * 【initWiFi() 函數內部執行步驟】
 *   a. 設定與 WiFi 模組通訊的序列埠 (Serial1) 速率為 115200 bps
 *   b. 發送 "AT" 指令測試模組是否正常回應
 *   c. 設定 WiFi 模式為 Station (客戶端模式)
 *   d. 連接到預設的 WiFi 熱點 (SSID 和密碼預設於 TCP.h 中)
 *   e. 取得並儲存 SSID、IP 位址、MAC 位址等資訊
 * 
 * 【SendtoClouding() 函數說明 (JSON 版本)】
 *   此函數定義於 clouding.h 中，負責將裝置資訊以 JSON 格式上傳至雲端：
 *   
 *   ┌─────────────────────────────────────────────────────────┐
 *   │ JSON 欄位名稱         │ 說明                            │
 *   ├───────────────────────┼─────────────────────────────────┤
 *   │ device.mac            │ 裝置 MAC 位址（唯一識別碼）     │
 *   │ device.ssid           │ 當前連接的 WiFi 熱點名稱        │
 *   │ device.ip             │ DHCP 分配的 IP 位址             │
 *   │ device.gateway        │ 閘道器位址                      │
 *   │ device.type           │ 裝置類型 (rfid_gateway)         │
 *   │ device.version        │ 韌體版本號                      │
 *   │ device.timestamp      │ 註冊時間戳                      │
 *   │ device.status         │ 裝置狀態 (active/standby)       │
 *   └─────────────────────────────────────────────────────────┘
 * 
 *   HTTP 請求範例：
 *   POST /api/device/register HTTP/1.1
 *   Host: api.example.com
 *   Content-Type: application/json
 *   Content-Length: 256
 *   
 *   {
 *     "mac": "AA:BB:CC:DD:EE:FF",
 *     "ssid": "Office_WiFi",
 *     "ip": "192.168.1.100",
 *     "type": "rfid_gateway",
 *     "version": "1.0.0"
 *   }
 * 
 * 【注意事項】
 *   1. 使用前請確認 TCP.h 中已正確設定 WiFi 的 SSID 和密碼
 *   2. 使用前請確認 clouding.h 中已正確設定雲端伺服器 URL
 *   3. 雲端伺服器需支援 JSON 格式解析和 HTTP POST 請求
 *   4. MAC 位址為裝置唯一識別碼，請確保雲端後台已正確處理
 *   5. 序列埠監控視窗速率請設定為 9600 bps
 *   6. 本版本為單次註冊模式，開機註冊一次後即停止
 *   7. 若需定期重新註冊或回報狀態，可啟用 loop 中的註解程式碼
 * 
 * 【修改記錄】
 *   版本: v1.0
 *   日期: 2024
 *   說明: RFID 門禁裝置雲端註冊程式 (JSON 格式版本)
 * 
 ******************************************************************/

#include <String.h>      // 引入字串處理函式庫，用於字串操作
#include "TCP.h"         // 引入 TCP 控制邏輯相關函式與變數定義
                         // 包含以下功能：
                         // - initWiFi(): WiFi 模組初始化與連線
                         // - GetSSID(): 取得當前連接的 WiFi 熱點名稱
                         // - GetIP(): 取得裝置的 IP 位址
                         // - GetMAC(): 取得 WiFi 模組的 MAC 位址
                         // - GetGateWay(): 取得閘道器位址
                         // - Wifi.getStatus(): 檢查 WiFi 連線狀態
#include "clouding.h"    // 引入 HTTP POST / JSON 相關函式庫
                         // 包含以下功能：
                         // - SendtoClouding(): 將 JSON 格式資料上傳至雲端伺服器
                         // - 組裝 JSON 格式的註冊資料
                         // - 發送 HTTP POST 請求
                         // - 處理伺服器回應

// 全域變數定義
// MacData: 儲存裝置的 MAC 位址，作為雲端註冊的唯一識別碼
// 此變數在 setup() 中由 GetMAC() 函數賦值，供 SendtoClouding() 使用
// 注意：clouding.h 中應已宣告此變數（extern String MacData）

// ------- 自定義函式宣告區 -----------
// 此處預留給使用者自定義函式宣告
// 例如：
// - buildJSONData(): 組裝 JSON 資料字串
// - parseServerResponse(): 解析伺服器回應
// - handleRegistrationError(): 處理註冊錯誤

//--------------------------------------------

/**
 * setup 函數：Arduino 程式啟動時只執行一次
 * 
 * 功能說明：
 *   1. 初始化硬體介面（LED 指示燈、USB 序列埠）
 *   2. 啟動 WiFi 模組並連接到預設熱點
 *   3. 顯示網路連線資訊（SSID、IP、MAC）
 *   4. 執行雲端註冊，將裝置資訊以 JSON 格式上傳
 * 
 * 執行時機：Arduino 開機或重置後自動執行
 */
void setup() 
{
  // LED 初始化設定
  digitalWrite(LED, LOW);   // 將 LED 腳位設定為 LOW（熄滅）
                            // LED 為低電位觸發（共陽極）或高電位點亮需視硬體而定
                            // 此處設定熄滅作為開機狀態指示，表示系統已啟動
                            // 後續可擴充：註冊成功時點亮，失敗時閃爍

  // USB 序列埠初始化（監控用）
  Serial.begin(9600);       // 啟動主機序列埠（USB）通訊，速率為 9600 bps
                            // 用途：
                            // 1. 輸出註冊過程資訊至電腦的序列埠監控視窗
                            // 2. 顯示 WiFi 連線狀態和註冊結果
                            // 3. 提供除錯資訊輸出，便於查看 JSON 資料內容
                            // 
                            // 注意：此速率需與序列埠監控視窗設定的速率一致

  // WiFi 模組初始化與連線
  initWiFi();               // 執行 WiFi 模組初始化與連線
                            // 此函數定義於 TCP.h 中，內部執行以下操作：
                            // 
                            // 【步驟 1】設定通訊序列埠
                            //   - 使用 Serial1（硬體序列埠）與 BMC81M001 模組通訊
                            //   - 通訊速率設定為 115200 bps
                            //
                            // 【步驟 2】模組功能測試
                            //   - 發送 "AT" 指令，確認模組正常回應
                            //   - 若無回應則重試，並輸出錯誤訊息
                            //
                            // 【步驟 3】設定 WiFi 工作模式
                            //   - 發送 "AT+CWMODE=1" 設定為 Station 模式
                            //   - Station 模式：作為客戶端連接其他 WiFi 熱點
                            //
                            // 【步驟 4】連接到 WiFi 熱點
                            //   - 發送 "AT+CWJAP=\"SSID\",\"密碼\"" 指令
                            //   - SSID 和密碼定義於 TCP.h 中的巨集
                            //   - 等待模組回應連線結果
                            //
                            // 【步驟 5】獲取網路資訊
                            //   - 發送 "AT+CWJAP?" 取得當前連線的 SSID
                            //   - 發送 "AT+CIFSR" 取得 IP 位址
                            //   - 發送 "AT+CIPSTAMAC?" 取得 MAC 位址
                            //   - 儲存這些資訊供後續 JSON 資料組裝使用

  // 輸出空行，用於格式美觀
  Serial.println("");       // 在開機資訊與後續輸出之間增加間隔
                            // 使輸出結果更清晰易讀

  // 顯示 WiFi 熱點資訊區塊
  Serial.println("---wifi access point----"); // 輸出分隔線，標示 WiFi 熱點資訊區塊
  Serial.println(GetSSID()); // 取得並輸出目前連接的 WiFi 熱點名稱（SSID）
                             // 確認裝置連接到正確的無線網路
                             // 此資訊將包含在 JSON 註冊資料中
                             // 便於雲端後台了解裝置所處的網路環境
  
  // 註解掉的除錯語句說明
  // Serial.println(typeof(Wifi.SSID())) ; 
  // 這行是開發階段的除錯程式碼，用於查詢 SSID 函數的回傳型別
  // 正式版本已註解，避免編譯錯誤或不必要的輸出

  // 顯示 IP 位址資訊區塊
  Serial.println("---Show IP Address----"); // 輸出分隔線，標示 IP 位址資訊區塊
  Serial.println(GetIP()); // 取得並輸出 DHCP 分配的 IP 位址
                           // 確認裝置成功取得 IP，表示網路通訊正常
                           // 此資訊將包含在 JSON 註冊資料中
                           // 雲端後台可透過此 IP 與裝置通訊
                           // 常見 IP 範例：192.168.1.100

  // 取得並儲存 MAC 位址
  MacData = GetMAC();       // 取得裝置的 MAC 位址（網路卡硬體位址）
                            // 將 MAC 位址存入全域變數 MacData
                            // 此變數將用於雲端註冊，作為裝置唯一識別碼
                            // GetMAC() 函數執行流程：
                            //   1. 發送 AT+CIPSTAMAC? 指令
                            //   2. 解析回應，格式如：+CIPSTAMAC:"AA:BB:CC:DD:EE:FF"
                            //   3. 回傳 MAC 位址字串
                            // 
                            // 在 JSON 版本中，MAC 位址是 JSON 物件的關鍵欄位
                            // 雲端後台會以此欄位作為資料庫的主鍵或唯一索引
  
  // 顯示 MAC 位址資訊區塊
  Serial.println("---MAC Address----"); // 輸出分隔線，標示 MAC 位址資訊區塊
  Serial.println(MacData); // 輸出 MAC 位址
                           // 輸出範例：AA:BB:CC:DD:EE:FF
                           // 此位址將作為 JSON 註冊資料中的 device.mac 欄位值

  // 檢查 WiFi 連線狀態並執行雲端註冊
  if (Wifi.getStatus())      // 檢查 WiFi 連線狀態是否正常
                             // getStatus() 回傳 true 表示連線正常
  {
    Serial.println("WIFI OK");    // 輸出 WiFi 連線正常訊息
    
    // 呼叫雲端註冊函數（JSON 版本）
    SendtoClouding();             // 此函數定義於 clouding.h 中
                                  // JSON 版本的 SendtoClouding() 執行以下操作：
                                  // 
                                  // 【步驟 1】組裝 JSON 資料
                                  //   - 建立 JSON 物件
                                  //   - 填入 MAC 位址、SSID、IP 等資訊
                                  //   - 範例：
                                  //     String jsonData = "{";
                                  //     jsonData += "\"mac\":\"" + MacData + "\",";
                                  //     jsonData += "\"ssid\":\"" + GetSSID() + "\",";
                                  //     jsonData += "\"ip\":\"" + GetIP() + "\"";
                                  //     jsonData += "}";
                                  // 
                                  // 【步驟 2】建立 HTTP POST 連線
                                  //   - 使用 AT+CIPSTART 指令建立 TCP 連線
                                  //   - 連線到雲端伺服器的指定埠號（通常為 80 或 443）
                                  // 
                                  // 【步驟 3】發送 HTTP 請求
                                  //   - 格式：
                                  //     POST /api/device/register HTTP/1.1
                                  //     Host: api.example.com
                                  //     Content-Type: application/json
                                  //     Content-Length: <JSON 長度>
                                  //     Connection: close
                                  //     <空行>
                                  //     <JSON 資料>
                                  // 
                                  // 【步驟 4】接收並處理回應
                                  //   - 讀取伺服器回應
                                  //   - 解析 JSON 格式的回應（如：{"status":"success","device_id":123}）
                                  //   - 輸出註冊結果到 Serial
                                  // 
                                  // 【步驟 5】關閉連線
                                  //   - 發送 AT+CIPCLOSE 指令
  }
  // 注意：若 WiFi 連線失敗，不會執行註冊
  // 可在此處加入錯誤處理，如 LED 閃爍指示錯誤
  // 也可將錯誤資訊以 JSON 格式記錄，供後續除錯
}

/**
 * loop 函數：Arduino 主程式迴圈
 * 
 * 功能說明：
 *   目前此函數中的程式碼被註解掉，表示此版本僅在開機時註冊一次
 *   若需定期重新註冊或回報狀態（以 JSON 格式），可取消註解並調整延遲時間
 * 
 * 【為什麼要註解？】
 *   1. 單次註冊設計：門禁裝置通常只需註冊一次即可
 *   2. 節省資源：避免頻繁的 HTTP 請求造成伺服器負擔
 *   3. 省電模式：適合電池供電的裝置
 *   4. JSON 資料量較大，頻繁發送可能消耗較多流量
 * 
 * 【可擴充功能說明（註解部分）】
 * 
 * 以下為註解掉的功能，可視需求啟用：
 * 
 * // 定時註冊/狀態回報功能（JSON 格式）
 * // 適用場景：需要定期更新裝置狀態，或確保裝置持續在線
 * // 啟用後，裝置會每 2 分鐘檢查一次 WiFi 狀態並以 JSON 格式上傳裝置資訊
 * 
 * // if (Wifi.getStatus())
 * // {
 * //    Serial.println("WIFI OK") ;
 * //    SendtoClouding() ;    // 以 JSON 格式傳送註冊/狀態資料到雲端
 * // }
 * // else
 * // {
 * //    Serial.println("WIFI not OK，Reconnect WIFI") ;
 * //    initWiFi();              // 重新執行 WiFi 模組初始化與連線
 * //    MacData = GetMAC() ;    // 重新取得 MAC 位址
 * //    Serial.println("---MAC Address----"); // 輸出分隔線
 * //    Serial.println(MacData); // 輸出 MAC 位址
 * //    SendtoClouding() ;      // 重新連線後以 JSON 格式傳送註冊資料到雲端
 * // }
 * // delay(120000) ;            // 延遲 120 秒（2 分鐘）後再次執行
 */
void loop() 
{
  // 目前未定義任何行為
  // 程式執行完 setup 後會停留在這裡，不做任何重複動作
  // 這樣設計適合只需要執行一次註冊的任務（門禁裝置初始化）
  
  // 若要實現定期註冊/狀態回報功能，請參考上方註解部分的程式碼
  
  // 可擴充的其他功能：
  // - 定期以 JSON 格式檢查 WiFi 連線狀態，斷線時自動重連
  // - 接收 RFID 讀卡機的資料，將刷卡記錄以 JSON 格式上傳到雲端
  // - 接收雲端下發的 JSON 格式權限更新指令並解析
  // - 將裝置狀態變化（如 IP 變更）以 JSON 格式主動回報
  // - LED 狀態指示（連線成功、註冊成功、註冊失敗等）
  // - 將 JSON 資料儲存到 SD 卡，作為離線備份
}

/**
 * 【補充說明：JSON 格式門禁裝置註冊詳解】
 * 
 * 1. JSON 資料結構設計範例
 * 
 *    {
 *      "device": {
 *        "mac": "AA:BB:CC:DD:EE:FF",
 *        "ssid": "Office_WiFi",
 *        "ip": "192.168.1.100",
 *        "gateway": "192.168.1.1",
 *        "subnet": "255.255.255.0"
 *      },
 *      "info": {
 *        "type": "rfid_gateway",
 *        "version": "1.0.0",
 *        "hardware": "BMduino_V2",
 *        "firmware_date": "2024-01-15"
 *      },
 *      "status": {
 *        "uptime": 3600,
 *        "signal_strength": -45,
 *        "last_heartbeat": "2024-01-15T10:30:00Z"
 *      }
 *    }
 * 
 * 2. 雲端伺服器 JSON 回應範例
 * 
 *    成功回應：
 *    {
 *      "status": "success",
 *      "code": 200,
 *      "data": {
 *        "device_id": 12345,
 *        "registered_at": "2024-01-15T10:30:00Z",
 *        "message": "Device registered successfully"
 *      }
 *    }
 * 
 *    失敗回應：
 *    {
 *      "status": "error",
 *      "code": 400,
 *      "message": "Invalid MAC address format"
 *    }
 * 
 * 3. JSON 解析方式（在雲端後台）
 * 
 *    // Node.js 範例
 *    app.post('/api/device/register', (req, res) => {
 *      const { mac, ssid, ip, type, version } = req.body;
 *      if (!mac || !ssid) {
 *        return res.status(400).json({ status: 'error', message: 'Missing required fields' });
 *      }
 *      // 儲存到資料庫
 *      db.devices.insertOne({ mac, ssid, ip, type, version, registered_at: new Date() });
 *      res.json({ status: 'success', device_id: result.insertedId });
 *    });
 * 
 * 4. JSON 格式的優點在門禁系統中的應用
 * 
 *    ┌─────────────────────────────────────────────────────────┐
 *    │ 應用場景              │ JSON 的優勢                      │
 *    ├───────────────────────┼─────────────────────────────────┤
 *    │ 多裝置批量註冊        │ 可一次上傳多個裝置的 JSON 陣列  │
 *    │ 權限同步              │ 可使用巢狀結構表達複雜權限規則  │
 *    │ 事件記錄              │ 可包含多種欄位，便於日後分析    │
 *    │ 配置下發              │ JSON 結構可完整表達裝置配置      │
 *    │ 版本相容性            │ 新增欄位不影響舊版裝置          │
 *    └─────────────────────────────────────────────────────────┘
 * 
 * 5. 完整的門禁系統 JSON 通訊流程
 * 
 *    裝置 (BMduino)                        雲端伺服器
 *          │                                    │
 *          │─── POST /api/device/register ────►│
 *          │    {                              │
 *          │      "mac": "AA:BB:CC:DD:EE:FF",  │
 *          │      "ssid": "Office_WiFi",       │
 *          │      "type": "rfid_gateway"       │
 *          │    }                              │
 *          │                                    │
 *          │◄─── 200 OK ────────────────────────│
 *          │    {                              │
 *          │      "status": "success",         │
 *          │      "device_id": 12345           │
 *          │    }                              │
 *          │                                    │
 *          │─── POST /api/device/heartbeat ───►│
 *          │    {                              │
 *          │      "mac": "AA:BB:CC:DD:EE:FF",  │
 *          │      "status": "active",          │
 *          │      "uptime": 3600               │
 *          │    }                              │
 *          │                                    │
 *          │◄─── 200 OK ────────────────────────│
 *          │    {                              │
 *          │      "status": "success"          │
 *          │    }                              │
 * 
 * 6. 常見問題與解決方案
 * 
 *    【問題 1】JSON 資料格式錯誤導致伺服器解析失敗
 *    - 原因：缺少引號、逗號位置錯誤、巢狀結構不完整
 *    - 解決：使用 Serial 輸出 JSON 字串進行檢查，確認格式正確
 * 
 *    【問題 2】HTTP POST 請求被拒絕
 *    - 原因：Content-Type 標頭未正確設定
 *    - 解決：確認 clouding.h 中已設定 "Content-Type: application/json"
 * 
 *    【問題 3】JSON 資料過長超出緩衝區
 *    - 原因：JSON 資料超過 WiFi 模組的發送緩衝區大小
 *    - 解決：簡化 JSON 結構，或使用分段發送
 * 
 *    【問題 4】伺服器回應無法解析
 *    - 原因：回應格式非預期的 JSON 結構
 *    - 解決：增加錯誤處理，記錄原始回應內容
 * 
 * 7. 程式輸出範例（JSON 版本）
 * 
 *    (開機後序列埠監控視窗顯示)
 *    
 *    ---wifi access point----
 *    Office_WiFi
 *    ---Show IP Address----
 *    192.168.1.100
 *    ---MAC Address----
 *    AA:BB:CC:DD:EE:FF
 *    WIFI OK
 *    (SendtoClouding 內部輸出)
 *    Sending JSON: {"mac":"AA:BB:CC:DD:EE:FF","ssid":"Office_WiFi","ip":"192.168.1.100","type":"rfid_gateway"}
 *    Server response: {"status":"success","device_id":12345}
 *    Registration completed!
 * 
 * 8. 安全建議
 * 
 *    - 使用 HTTPS 加密傳輸 JSON 資料
 *    - 在 JSON 中加入時間戳記和簽章，防止重放攻擊
 *    - 敏感資訊（如密碼）不應明文傳輸
 *    - 伺服器端應驗證 JSON 結構的完整性
 *    - 定期更換 API 金鑰或 Token
 */